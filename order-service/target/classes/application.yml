server:
  port: 8089

spring:
  application:
    name: order-service
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce_order?createDatabaseIfNotExist=true
    username: root
    password: ${DB_PASSWORD:anmol432006}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  mail:
    host: ${SMTP_HOST:smtp.gmail.com}
    port: ${SMTP_PORT:587}
    username: ${SMTP_USERNAME:anmolprajapati802@gmail.com}
    password: ${SMTP_PASSWORD:uckbgresxhwltxya}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000

app:
  razorpay:
    key-id: ${RAZORPAY_KEY_ID:rzp_test_SoJh85MrdD59Ff}
    key-secret: ${RAZORPAY_KEY_SECRET:lDiMQd3fHDOCqJMx66CrEyCc}
    currency: ${RAZORPAY_CURRENCY:INR}
    mock: ${RAZORPAY_MOCK:false}
  mail:
    enabled: ${MAIL_ENABLED:true}
    from: ${MAIL_FROM:anmolprajapati802@gmail.com}
  support:
    email: ${SUPPORT_EMAIL:support@mystore.local}
