databases:
  - name: mystore-db
    databaseName: ecommerce
    user: root
    plan: free

envVarGroups:
  - name: shared-secrets
    envVars:
      - key: JWT_SECRET
        generateValue: true

services:
  # 1. Discovery Service
  - type: web
    name: discovery-service
    runtime: docker
    dockerContext: .
    dockerfile: Dockerfile
    plan: free
    healthCheckPath: /
    envVars:
      - key: SERVICE_NAME
        value: discovery-service

  # 2. Auth Service
  - type: web
    name: auth-service
    runtime: docker
    dockerContext: .
    dockerfile: Dockerfile
    plan: free
    envVars:
      - key: SERVICE_NAME
        value: auth-service
      - key: SPRING_DATASOURCE_URL
        value: jdbc:mysql://mystore-db:3306/ecommerce_auth?createDatabaseIfNotExist=true
      - key: SPRING_DATASOURCE_PASSWORD
        fromDatabase:
          name: mystore-db
          property: password
      - key: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
        value: http://discovery-service:8761/eureka/
      - fromGroup: shared-secrets

  # 3. Product Service
  - type: web
    name: product-service
    runtime: docker
    dockerContext: .
    dockerfile: Dockerfile
    plan: free
    envVars:
      - key: SERVICE_NAME
        value: product-service
      - key: SPRING_DATASOURCE_URL
        value: jdbc:mysql://mystore-db:3306/ecommerce_product?createDatabaseIfNotExist=true
      - key: SPRING_DATASOURCE_PASSWORD
        fromDatabase:
          name: mystore-db
          property: password
      - key: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
        value: http://discovery-service:8761/eureka/

  # 4. Order Service
  - type: web
    name: order-service
    runtime: docker
    dockerContext: .
    dockerfile: Dockerfile
    plan: free
    envVars:
      - key: SERVICE_NAME
        value: order-service
      - key: SPRING_DATASOURCE_URL
        value: jdbc:mysql://mystore-db:3306/ecommerce_order?createDatabaseIfNotExist=true
      - key: SPRING_DATASOURCE_PASSWORD
        fromDatabase:
          name: mystore-db
          property: password
      - key: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
        value: http://discovery-service:8761/eureka/
      - key: SMTP_HOST
        sync: false
      - key: SMTP_PORT
        sync: false
      - key: SMTP_USERNAME
        sync: false
      - key: SMTP_PASSWORD
        sync: false
      - key: RAZORPAY_KEY_ID
        sync: false
      - key: RAZORPAY_KEY_SECRET
        sync: false

  # 5. Gateway Service
  - type: web
    name: gateway-service
    runtime: docker
    dockerContext: .
    dockerfile: Dockerfile
    plan: free
    envVars:
      - key: SERVICE_NAME
        value: gateway-service
      - key: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
        value: http://discovery-service:8761/eureka/
      - fromGroup: shared-secrets

  # 6. Frontend (Using its own Dockerfile in its folder)
  - type: web
    name: mystore-frontend
    runtime: docker
    dockerContext: ./frontend
    plan: free
    envVars:
      - key: VITE_API_GATEWAY_URL
        fromService:
          name: gateway-service
          type: web
          property: host
